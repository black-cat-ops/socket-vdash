apiVersion: 1

providers:
  - name: Socket VDash
    orgId: 1
    folder: Socket Security
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
