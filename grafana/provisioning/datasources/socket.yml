apiVersion: 1

datasources:
  - name: Socket VDash
    type: postgres
    url: postgres:5432
    user: socket
    secureJsonData:
      password: changeme
    jsonData:
      database: socket_vdash
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 2
      connMaxLifetime: 14400
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
    editable: true
